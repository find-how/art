# Pioneer Landing Page Copy

## Eyebrow

Pioneer by find.how

## H1

The full-stack TypeScript framework that compiles to the edge.

## Supporting Copy

Write application logic in TypeScript. Pioneer wires the Cloudflare Worker, D1 database, cache, queue, auth, logs, and deploy path around it.

## Primary CTA

Deploy to Pioneer

## Secondary CTA

View docs

## Demo Proof

Ask AI for a checkout endpoint. Pioneer turns the generated route into a running Cloudflare-shaped app with local Wrangler feedback.

## Proof Cards

| Label | Copy |
|-------|------|
| Route | `POST /checkout` becomes the Worker HTTP entrypoint. |
| D1 | `DB.table("orders")` becomes the database binding. |
| Cache | `Cache.set(...)` becomes edge cache work. |
| Queue | `Queue.dispatch("send-receipt")` becomes a background job. |
| Wrangler | The local smoke request returns `201 Created`. |

## Avoid

Do not make the first viewport a generic SaaS hero. The code editor and deploy feedback are the stage.
