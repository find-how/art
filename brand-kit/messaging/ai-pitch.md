# AI Pitch

AI writes code. Pioneer makes it deployable.

The Demo starts with AI because AI is now how many developers begin a feature. Pioneer should not be positioned as an AI code generator. Its value is what happens next: the generated code becomes a running app with real Cloudflare-shaped services.

## Approved AI Copy

- Ask AI for the endpoint. Pioneer turns the generated code into the app.
- AI generated the business logic. Pioneer ran the app.
- Generated code still needs infrastructure. Pioneer supplies the production shape.
